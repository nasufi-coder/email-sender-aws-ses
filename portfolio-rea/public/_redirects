# Proxy to Patrik's portfolio (keeps domain)
/patrik-nasufi/* https://patrik-nasufi.netlify.app/:splat 200

# Proxy to Prolder Company main site (keeps domain)
/prolder-company/* https://prolder-solutions.netlify.app/:splat 200

# Reinalda's portfolio (local content)
/reinalda-radomi/* /:splat 200

# Fallback for SPA
/*    /index.html   200