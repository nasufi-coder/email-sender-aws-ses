# Proxy to Patrik's portfolio (keeps domain)
/patrik-nasufi/* https://patrik-nasufi.netlify.app/:splat 200

# Proxy to Reinalda's portfolio (keeps domain)
/reinalda-radomi/* https://reinalda-radomi.netlify.app/:splat 200

# Main company site (root domain)
/ /index.html 200

# Fallback for SPA
/*    /index.html   200